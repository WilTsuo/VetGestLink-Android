<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Altere o 'parent' para Theme.MaterialComponents.DayNight.NoActionBar -->
    <style name="Theme.VetGestLink" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Personalização de cores (ajuste conforme as cores do seu projeto) -->
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorSecondary">@color/primary_green</item>
        <item name="colorOnSecondary">#FFFFFF</item>

        <!-- Cor da barra de status -->
        <item name="android:statusBarColor">@color/primary_green</item>

        <!-- Outras configurações de estilo podem permanecer aqui -->
    </style>
</resources>